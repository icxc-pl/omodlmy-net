use omodlmy-net

db.createCollection('sessions');

db.createCollection('intentions');
db.getCollection('intentions').createIndex({
  'authorId': 1
});

db.createCollection('stats');
db.getCollection('stats').insert({
  "intentions": 0,
  "praying": 0
});

exit
